package com.example.theworldrequiem.Data

class Level(pLevel: Int, pBackground: String, pTime: Int, pBoss: Boolean) {
    val level = pLevel
    val background = pBackground
    val time = pTime
    val boss = pBoss
}